{
    "name": "api.pxflux.xyz",
    "version": "0.0.0",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "clean": "rm -rf node_modules package-lock.json packages/*/{node_modules,package-lock.json,dist}",
        "publish": "npm run publish --workspaces --if-present"
    },
    "engines": {
        "node": "^16"
    }
}
